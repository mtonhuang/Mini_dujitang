    <navigator url="{{topic.article.article_label == '商品'?'../goods/detail/index?id=':'../article/single?id='}}{{topic.article.post_id}}" class="c-article">
        <view class="c-article__cover">
            <image class="c-article__poster" src="{{topic.article.article_thumb}}" background-size="cover">
                <view class="c-article__label is-post">{{topic.article.article_label}}</view>
            </image>
        </view>
        <view class="c-article__content">{{topic.article.article_title}}</view>
    </navigator>

